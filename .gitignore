# Environments
.venv/
node_modules/
web-client/node_modules/
chrome-extension/node_modules/

# Secrets / Config
.env # Ignore root .env
# Explicitly ignore local overrides
.env.local 
web-client/.env.local

# Add other specific .env files to ignore if necessary
# e.g., chrome-extension/.env.local

# --- Allow specific non-secret files (if they exist) ---
# !.env.example # Usually committed anyway
# !.env.test # If it contains no secrets
# !web-client/.env.development 
# !web-client/.env.production

# Certs (if self-signed and shouldn't be committed)
certs/
python_server/certs/
# certs/key.pem # Covered by certs/
# certs/cert.pem # Covered by certs/

# Python cache/artifacts
*.pyc
*.pyo
*.pyd
__pycache__/
.Python
.python-version
.pytest_cache/
python_server/venv/

# JS Build artifacts
dist/
web-client/build/

# Data Files (generated by app)
data/
python_server/storage/
# python_server/data/ # Redundant if data/ is ignored
# python_server/storage/ # Redundant if data/ is ignored

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / Editor specific
.DS_Store
.vscode/
.idea/
.cursor/
*.swp
*.swo

# Test Reports / Coverage
coverage/
*.lcov

# Next.js build output
.next/